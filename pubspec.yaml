name: application_dsm_v1
description: "Application mobile Librairie DSM."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  webview_flutter: ^4.8.0
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.1.2
  url_launcher: ^6.2.6
  shared_preferences: ^2.3.2
  cached_network_image: ^3.4.1
  flutter_local_notifications: ^18.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/images/
